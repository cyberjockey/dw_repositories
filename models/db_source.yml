version: 2

sources:
  - name: normalized_data_swavibr
    description: This is a replica of the Bigquery database used by our Data Warehouse
    tables:
      - name: accounts
        description: >
          One record per order. Includes cancelled and deleted orders.
        columns:
			- name : domestic_balance